{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "agent",
            "if": "Bash(git commit*)",
            "prompt": "Run pre-commit checks before the commit proceeds:\n1. Run `npm run lint` — if it fails, output the errors and block the commit\n2. Run tests: `npm run test -- --passWithNoTests 2>/dev/null || true` — if tests fail, report and block\n3. If all checks pass, allow the commit to proceed",
            "timeout": 120,
            "statusMessage": "Running pre-commit checks (lint + tests)..."
          }
        ]
      }
    ]
  }
}
